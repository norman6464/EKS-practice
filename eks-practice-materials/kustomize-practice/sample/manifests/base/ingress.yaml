apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-lb
  namespace: kustomize-sample
  labels:
    app: webapp
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-service
                port:
                  number: 8080
